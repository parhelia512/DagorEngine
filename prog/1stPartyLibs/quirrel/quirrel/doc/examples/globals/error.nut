println("stdout: starting")
error("stderr:", "something went wrong")  // several arguments join with one space, like print
errorln("")                                // error() itself adds no trailing newline
println("stdout: continuing")
