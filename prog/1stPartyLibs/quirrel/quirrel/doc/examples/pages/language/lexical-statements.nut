// ';' lets two statements share a line; a lone newline also ends one
let ammoLeft = 5; let magazineSize = 30

// a newline does not end a statement while an operator leaves it dangling
let totalRounds = ammoLeft +
  magazineSize

println(totalRounds)
