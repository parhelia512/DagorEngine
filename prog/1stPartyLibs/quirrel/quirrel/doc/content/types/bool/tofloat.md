---
see_also: [types.Bool.tointeger, types.Bool.tostring, types.Integer.tofloat]
---

Converts the bool to a float.

## Return value

`1.0` for `true`, `0.0` for `false` - both print with no decimal point, see
[`tostring`](sym:types.Float.tostring).

## Notes

Takes no arguments.

## Example

{{example:types.Bool.tofloat}}
