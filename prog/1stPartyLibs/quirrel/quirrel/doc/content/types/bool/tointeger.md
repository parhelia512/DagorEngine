---
see_also: [types.Bool.tofloat, types.Bool.tostring, types.Integer.tointeger]
---

Converts the bool to an integer.

## Return value

`1` for `true`, `0` for `false`.

## Notes

Takes no arguments, the same fixed arity as
[`types.Integer.tointeger`](sym:types.Integer.tointeger); there is no `base`
parameter here either.

## Example

{{example:types.Bool.tointeger}}
