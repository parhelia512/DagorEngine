---
see_also: [types.Array.resize, types.Array.top]
---

Returns the number of elements in the array.

## Return value

The element count, as a non-negative `int`.

## Example

{{example:types.Array.len}}
