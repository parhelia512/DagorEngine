---
see_also: [types.Array.pop, types.Array.len]
---

Returns the last element without removing it.

## Return value

The last element.

## Errors

Throws `top() on a empty array` when the array has no elements.

## Notes

[pop](sym:types.Array.pop) reads the same element and removes it.

## Example

{{example:types.Array.top}}
