---
see_also: [types.Array.top, types.Array.append]
---

Removes and returns the last element.

## Return value

The removed element.

## Errors

Throws `empty array` when the array has no elements.

## Notes

[top](sym:types.Array.top) reads the same element without removing it.

## Example

{{example:types.Array.pop}}
