---
params: [other]
see_also: [types.Array.replace_with]
---

Deprecated alias of [replace_with](sym:types.Array.replace_with).

## Parameters

- `other` - the array to copy elements from

## Return value

This array.

## Notes

`replace` and `replace_with` are the exact same native function registered
under two names; use `replace_with`, the newer name, in new code.

## Example

{{example:types.Array.replace}}
