---
see_also: [math.cos, math.tan, math.asin, math.PI]
---

Returns the sine of `x`, given in radians.

## Parameters

- `x` - angle in radians

## Return value

The sine of `x`, always as a float.

## Notes

`x` is defined for any real number; there is no restricted domain to worry
about, unlike [asin](sym:math.asin) and [acos](sym:math.acos).

## Example

{{example:math.sin}}
