---
see_also: [math.FLT_MIN]
---

The largest finite value `SQFloat` can represent.

## Notes

Taken directly from the C `FLT_MAX` macro in `<float.h>`, cast to `SQFloat`.
`-FLT_MAX` is the most negative finite value, not `FLT_MIN` - see the Notes on
that page.
