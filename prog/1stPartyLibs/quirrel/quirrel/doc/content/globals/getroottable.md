---
see_also: [getconsttable, compilestring]
---

Returns the root table.

## Return value

The root table itself, the same object that is available using `::` operator.
`::foo` is equivalent to `getroottable()["foo"]`.

## Example

{{example:getroottable}}
