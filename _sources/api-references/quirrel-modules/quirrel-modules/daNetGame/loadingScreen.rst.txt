.. autogenerated

*************
loadingScreen
*************

module 'loadingScreen'



*Source files:*

*prog/daNetGame/render/animatedSplashScreenSq.cpp*


Usage::

  let { set_loading_screen_config } = require("loadingScreen")

or::

  from "loadingScreen" import *

.. code-block:: sq

    //'loadingScreen' exports:
    {
      function set_loading_screen_config(cfg): instance|null -> null
    }
    

.. sq:function:: function set_loading_screen_config(cfg)

  Scene config for the animated loading splash starts that follow: cfg is a DataBlock with the settings.blk

  :param cfg:  
  :type cfg: instance|null

  ``nparamscheck:1``

  ``typecheck mask: class_instance or null``

  :return: null

loadingScreen{} layout (screens{}, default, mode, exact, chosen); null or an empty block returns to settings.blk.
Keys the block lacks still come from settings.blk, so a list-only block keeps the user's mode, exact and chosen
keys, though a name they pick that the config does not list falls back to its default. graphDir is not layered:
naming it here is ignored. The block is copied, so a later change to cfg needs another call. Safe from any thread.


